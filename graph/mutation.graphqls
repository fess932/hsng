type Mutation {
    createTodo(input: NewTodo!): Todo!
    sendMessage(input: NewMessage!): Message!

    "Sign up ?"
    createUser(input: NewUser!): User!

    "add another user to friend list, returns updated current user info"
    addFriend(input: ID): User!
}